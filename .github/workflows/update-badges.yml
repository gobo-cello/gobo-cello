name: Update certifications badges

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  update-badges:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Update badges
        uses: pemtajo/badge-readme@main
        with:
          CREDLY_USER: username.3cb9ef81
